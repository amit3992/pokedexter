<div class="max-w-2xl mx-auto mt-8 space-y-4">
  <div class="flex justify-between items-center">
    <h1 class="text-2xl font-bold text-gray-800 dark:text-white">Your Pokémon Collection</h1>
    <a href="/" class="px-5 py-2.5 bg-blue-500 hover:bg-blue-600 text-white font-medium rounded-xl shadow-lg hover:shadow-xl transition-all duration-200 flex items-center gap-3">
      <span class="text-xl">🎮</span> <span class="px-1">Home</span>
    </a>
  </div>
  <% if @caught.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <% @caught.each do |p| %>
        <div class="p-4 rounded-2xl shadow-lg border border-gray-100 dark:border-gray-700 bg-white dark:bg-gray-800 flex items-center gap-4 hover:shadow-xl transition-all duration-200">
          <img src="<%= p.sprite_url %>" alt="<%= p.name %>" class="w-16 h-16 p-2 bg-gray-50 dark:bg-gray-700 rounded-xl object-contain" />
          <div class="flex-1">
            <div class="font-bold text-gray-800 dark:text-white"><%= p.name.capitalize %> <span class="text-sm font-normal text-gray-500 dark:text-gray-400">#<%= p.poke_id %></span></div>
            <div class="text-sm text-gray-600 dark:text-gray-300">Base EXP: <span class="font-medium"><%= p.base_experience %></span></div>
            <div class="text-xs text-gray-500 dark:text-gray-400 mt-1">Caught: <%= p.caught_at&.strftime("%b %d, %Y %H:%M") %></div>
          </div>
          <%= button_to release_pokemon_path(p), method: :delete, form: { class: "ml-2" }, class: "px-3 py-1.5 bg-red-500 hover:bg-red-600 text-white text-sm font-medium rounded-lg shadow hover:shadow-md transition-all duration-200", data: { confirm: "Are you sure you want to release #{p.name.capitalize}?" } do %>
            Release
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-12 bg-gray-50 dark:bg-gray-800 rounded-2xl">
      <div class="text-5xl mb-4">👁‍🗨</div>
      <p class="text-lg font-medium text-gray-700 dark:text-gray-300">You haven't caught any Pokémon yet!</p>
      <a href="/" class="mt-4 inline-block px-5 py-2.5 bg-red-500 hover:bg-red-600 text-white font-medium rounded-xl shadow-lg hover:shadow-xl transition-all duration-200">
        Go Catch Some Pokémon
      </a>
    </div>
  <% end %>
</div>